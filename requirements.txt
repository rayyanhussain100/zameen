# Mirrors [project.dependencies] in pyproject.toml, for pip-only workflows
# (`pip install -r requirements.txt`) as an alternative to `pip install -e .`.

google-adk>=0.1.0
google-genai>=0.3.0
httpx>=0.27
selectolax>=0.3.21
tenacity>=8.2
psycopg[binary]>=3.1
pgvector>=0.3.0
python-dotenv>=1.0
